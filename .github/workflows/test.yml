name: test

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    # runs-on: ubuntu-latest 이건 깃허브 내에서, 아래는 윈도우에서
    runs-on: self-hosted
    steps:
      # - uses: actions/checkout@v 이것도 깃허브 내에서 돌아갈 때
      # - name: Check PowerShell
      #   run: $PSVersionTable
      - name: Checkout repo
        uses: actions/checkout@v4
        # with:
        #   path: